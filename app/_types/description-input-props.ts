type DescriptionInputProps = {
  label?: string;
  rows?: number;
  maxLength?: number;
  register: object;
  currentlength: number;
  error: boolean;
  helperText: string;
};

export default DescriptionInputProps;
