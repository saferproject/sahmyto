# Apply Front-end Fixes (skip activities feature entirely)

Continue on branch `refactor/reusable-components`. No API/back-end changes. Do NOT touch any activities-list files or activity services.

## 1. Quick correctness fixes
- `next.config.ts`: `/dashboard` redirect `permanent: true` → `false`.
- Flip remaining 7 loose `==` to `===` (payment drawer/item, partner-form, income-form, use-can-approve — all number-vs-number, verified safe; none in activities files).
- Naming/typo cleanup (internal only):
  - `PartnersListService`→`partnersListService`, `DriversListService`→`driversListService`, `IncomeListService`→`incomeListService`, `ExpensesListService`→`expensesListService` (camelCase majority).
  - `financialManagmentService`→`financialManagementService`; `rehect-payment-body.ts`→`reject-payment-body.ts`; profile `_services/profile-service.ts` export `loginService`→`profileService`; mutationKey `app-payment`→`add-payment`.

## 2. Auth UX: cookie + middleware + cross-tab logout
- New `app/_utilities/auth-session.ts`: `markAuthSession()` / `clearAuthSession()` managing a non-sensitive `sahmyto_auth=1` cookie (the real token stays in localStorage).
- Call `markAuthSession()` in verify-form login success and authentication-guard; `clearAuthSession()` in proxy `clearAuthStorage()` and header logout.
- `middleware.ts`: redirect `/dashboard/**` to `/login` when cookie absent (early redirect, no more unauthenticated flash).
- `authentication-guard.tsx`: listen to window `storage` event — if token removed in another tab, redirect to `/login`.

## 3. Security: CSP headers (production only)
- `next.config.ts` `headers()`: `default-src 'self'`; `script-src 'self' 'unsafe-inline' 'unsafe-eval'` in dev / `'self' 'unsafe-inline'` in prod; `style-src 'self' 'unsafe-inline'` (Emotion/Tailwind); `img-src 'self' data: blob:` + image host from `NEXT_PUBLIC_IMAGE_ASSETS_URL`; `connect-src 'self'` + API URL; `object-src 'none'`; `base-uri 'self'`; `frame-ancestors 'none'`.

## 4. Accessibility
- `SelectableTagComponent`: `<div onClick>` → `<button type="button">` (Tailwind preflight resets button styling).
- `data-list-item-component.tsx`: clickable row → `<button type="button">` with `w-full text-start`.
- `AnimatedListItem`: accept `role`, `tabIndex`, `onKeyDown` passthroughs; month-list-item gets `role="button" tabIndex={0}` + Enter/Space handler.

## 5. Error/loading boundaries
- Add `error.tsx` + `loading.tsx` (matching root styles, Persian copy) to `app/dashboard/` and `app/dashboard/karbooms/`.

## 6. Payment details drawer unification
- `EntityDetailsDrawerComponent`: render approvals section only when `approvals` prop is provided.
- Rewrite `payment-details-drawer-layout.tsx` on `EntityDetailsDrawerComponent` (no approvals prop, receiver-based permission via `approveConfig.canApprove`), replacing its bespoke SwipeableDrawer.

## 7. Bundle: lazy-load heavy deps
- Split `date-picker-component.tsx` into impl + `next/dynamic` re-export (`ssr: false`, skeleton loading) so `@mui/x-date-pickers` + jalaliday chunk splits from the main bundle.
- `profile-picture/page.tsx`: `next/dynamic` for the Cropper component (`react-easy-crop`) and dynamic `import("browser-image-compression")` at compress time.

## 8. Testing foundation + devtools
- Add Vitest (dev dep) with `pnpm test` script; unit tests for pure utilities: `format-dates`, `parse-numbers`, `format-numbers`, `create-query-params`, `is-valid-query-id`, `normalize-settlement-data`.
- Add `ReactQueryDevtools` in the query provider (development only).

## Verification
- `pnpm lint`, `pnpm build`, `pnpm test`; manual smoke via dev server (login flash, dashboard lists, payment drawer, insurance forms, profile picture page).
- Focused commits per group. Activities feature untouched.