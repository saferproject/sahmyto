# Refactor: Extract Reusable Components & Logic from Duplicated Code

Create branch `refactor/reusable-components` from `development` and implement all phases below, committing per phase. Fix the two discovered bugs (Math.max divergence in third-party insurance remaining-days; copy-pasted export/title in activity-form-drawer-component.tsx).

## Phase 1 — Utilities & foundations
1. `app/_utilities/format-dates.ts` — `formatDate(value, fmt = "YYYY/MM/DD")`; replace ~12+ inline `dayjs(x).format(...)` occurrences.
2. HTTP verb helpers wrapping `fetchWithAuth` (default JSON headers/body); migrate all ~20 service files.
3. `PriceWithUnit` component (formatNumber + toman image); use in income/expense/payment list items, data-list-item, wallet components.

## Phase 2 — Hooks & stores
4. `useInvalidatingMutation` in `app/_hooks`; migrate all 32 mutation hooks; normalize typing; verify suspicious `useQuery.invalidateQueries` in use-add-bonus-penalty-driver-endpoint.ts.
5. `useListQuery(key, serviceFn, karboomId, enabled?)` factory for ~14 list query hooks; normalize `enabled` guard.
6. `useZodForm(schema, defaults)`; migrate 14 form hooks.
7. `useCanApprove(approvals, status)` hook (5+ inline copies).
8. `createActiveEntityStore<T>(defaults)` factory for 6+ Zustand stores.
9. Generic approve/reject service + hooks for income/expense/payment triple.

## Phase 3 — Schemas
10. Merge body/third-party insurance schemas into shared `insurance-form-schema.ts`.
11. Shared zod field presets: `dayjsField`, `imageField`, `descriptionField`.

## Phase 4 — Shared UI components
12. `StatusChipComponent` + unify the two status-color maps; replace 9 inline usages.
13. `AnimatedListItem` (motion.li wrapper, 10 copies).
14. `FormDrawerWithTitle` collapsing 7 drawer wrappers; fix copy-paste export/title in activity-form-drawer.
15. Insurance feature merge (items, forms, drawers → shared generic components); unify remaining-days to `Math.max(0, ...)` guarded version.
16. `SelectableTag` with variant prop (4 chip/tile components).
17. `DescriptionBlock` ("توضیحات ثبت کننده" box; 5 usages).
18. Promote `income-detail-item-component` to `app/_components` as `DetailItemComponent`; relocate de-facto shared approval-item and search-input to stop cross-feature imports.

## Phase 5 — Structural consolidation
19. `EntityDetailsDrawer` generic details drawer consolidating the 3 drawer layouts; unify payment's SwipeableDrawer/permission divergence behind config.
20. `ApprovableListItem` for the 3 financial list items (~300 lines saved).
21. `EntityListLayout` for the 3 cloned list-page layouts.
22. `MemberListItem` / `useOwnerItemActions` for driver/partner items.

## Verification
Per phase: `pnpm lint`, `pnpm build`, prettier on changed files, manual smoke of affected routes. Focused imperative commits per phase. No API contract or dependency changes.